# Production Environment Variables Template
# Copy this to .env on your server or configure in Coolify

# --- Database ---
POSTGRES_USER=carscout_user
POSTGRES_PASSWORD=secure_password_change_me
POSTGRES_DB=carscout

# --- Security ---
# Generate a strong secret: openssl rand -base64 32
JWT_SECRET=change_this_to_a_very_long_random_string
JWT_EXPIRES_IN=7d

# --- URLs ---
# The URL where your frontend will be accessible
FRONTEND_URL=https://carscout.yourdomain.com
# The API URL for frontend build (if separate domains)
VITE_API_URL=https://api.carscout.yourdomain.com
# OR if using same domain with Nginx proxy (recommended for Coolify)
# VITE_API_URL=/api

# --- Analytics ---
ANALYTICS_DEFAULT_DAYS=30
ANALYTICS_MAX_DAYS=365
ANALYTICS_CACHE_TTL=300
# --- Financing ---
VEHIS_LOGIN=vehis@example.com
VEHIS_PASSWORD=secure_password
